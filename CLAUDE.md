# CLAUDE.md

## Repository Overview

This is a **GitHub profile repository** (`Kevinfarahmand/Kevinfarahmand`). GitHub treats this special repository's `README.md` as the public-facing profile page displayed at `github.com/Kevinfarahmand`.

The repository currently contains a single file:

| File | Purpose |
|------|---------|
| `README.md` | GitHub profile page content (auto-displayed on the profile) |

## Owner Interests & Context

- **Languages/Technologies**: Kotlin, Java, MATLAB
- **Domain**: Image processing
- **Learning**: Kotlin and Java
- **Collaboration**: Open to image processing projects
- **Contact**: hadifarahmandimotlagh@gmail.com

## Development Conventions

### README.md
- This file renders directly on the GitHub profile — keep it concise and accurate
- Supports full GitHub-Flavored Markdown (GFM) including badges, tables, and HTML
- Emoji shortcodes (`:wave:`) and Unicode emoji are both supported
- Changes are immediately visible on the profile once merged to `main`

### Branching
- `main` is the default and production branch — it is what renders on the profile
- Feature branches follow the pattern `claude/<description>` for AI-assisted changes

### Commit Style
- Single-commit changes are typical for this simple repo
- Descriptive present-tense messages (e.g., `Update README with new projects`)

## Common Tasks

**Update profile bio or interests**
Edit `README.md` directly — no build step required.

**Add GitHub stats badges or shields**
Insert Markdown image links referencing badge services (e.g., `shields.io`, `github-readme-stats`). Always use HTTPS URLs.

**Add project showcases**
Use a Markdown table or list in `README.md` linking to relevant repos.

## No Build System
There are no dependencies, package managers, CI pipelines, or test suites in this repository. All changes are pure Markdown edits.
